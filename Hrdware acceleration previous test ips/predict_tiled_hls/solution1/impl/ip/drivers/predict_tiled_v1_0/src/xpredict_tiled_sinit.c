// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xpredict_tiled.h"

extern XPredict_tiled_Config XPredict_tiled_ConfigTable[];

XPredict_tiled_Config *XPredict_tiled_LookupConfig(u16 DeviceId) {
	XPredict_tiled_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XPREDICT_TILED_NUM_INSTANCES; Index++) {
		if (XPredict_tiled_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XPredict_tiled_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XPredict_tiled_Initialize(XPredict_tiled *InstancePtr, u16 DeviceId) {
	XPredict_tiled_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XPredict_tiled_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XPredict_tiled_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

